# RestX 开发宗旨

本文件适用于整个 RestX 仓库。所有开发者和代码代理在设计、实现、重构、测试及评审代码时，都必须遵守以下原则。

## 1. 扩展性优先

RestX 会持续增加彼此差异较大的功能。新增代码必须符合当前的特性模块架构，不能为了快速交付而重新形成集中式、强耦合实现。

- 一个业务功能应归属于独立的 `src/features/<feature-id>/` 特性胶囊。
- 特性可以分别拥有 `renderer`、`main`、`preload`、`shared`、样式、状态和测试。
- 左侧菜单与页面路由由 renderer 特性声明统一生成。新增菜单不得在 Shell 或 Router 中加入特性专用判断。
- 需要系统权限的功能通过该特性自己的 main handler 和 preload 白名单 API 暴露，不得向 renderer 提供任意 IPC channel 调用能力。
- 特性的业务状态、样式、存储和异常处理应由该特性自行管理。单个特性发生错误时，不得影响其他菜单和应用外壳。
- 特性不得直接引用另一个特性的内部页面、组件、状态或服务。确需协作时，应通过公开契约或 capability，并显式声明依赖。
- `src/platform/` 只保留应用生命周期、特性注册、依赖校验、安全边界、错误隔离等架构能力，以及确定会被大多数特性依赖的稳定通用代码。
- 只被一个或少数特性使用的业务实现不得提前放入 platform 或创建万能 shared 模块。
- 新增或删除特性后，必须验证重复 id、重复路由、IPC channel、依赖缺失、依赖循环和跨特性私有导入。

详细目录与接入方式见 `docs/feature-architecture.md`。

## 2. 蓝区与黄区

RestX 的代码按开放范围分为蓝区和黄区。这是代码归属和信息边界，不代表两套不同的架构。

### 蓝区（开放区）

蓝区是默认区域，包含可公开、可复用、可审查的代码：

- 平台框架、特性注册机制和生命周期管理；
- 通用类型、接口、数据契约和 capability 定义；
- 通用 UI、基础组件、安全封装和基础工具；
- 不包含专有实现的业务流程骨架；
- 使用公开或合成数据的测试、示例和开发文档。

除非用户明确指定为黄区，新代码默认归入蓝区。

### 黄区（保密区）

黄区只承载少量必须保密的专有实现，例如特定算法、策略或实现性质的函数体。

- 黄区必须保持最小化，不得把完整特性、框架代码或通用能力整体放入黄区。
- 接口、类型和通用契约应定义在蓝区；黄区只实现这些契约。
- 黄区代码可以依赖蓝区契约，蓝区业务代码不得直接依赖或了解黄区实现细节。
- 黄区能力应通过依赖注入、窄接口或受控适配器接入。组合入口可以完成装配，但不得重新导出黄区内部实现。
- 不得将黄区源码、专有规则、真实样本或可推导实现细节的内容复制到蓝区。
- 不得把黄区内容写入日志、错误消息、遥测、测试快照、公开测试数据、注释、README、OpenSpec 或模型提示词。
- 黄区测试应留在相同保密边界内；蓝区只针对公开契约使用合成数据做黑盒验证。
- 已被标记为黄区的代码，不得在未经用户明确同意的情况下移动、改写为开放实现或扩大可见范围。

## 3. 边界判断

开始编码前，应先确认代码属于哪个特性、哪个进程以及哪个开放区域。

1. 先确定功能所有者，避免把业务代码放入 platform。
2. 再确定 renderer、main、preload 或 shared 边界。
3. 最后判断蓝区或黄区；无法确定时停止扩散，向用户确认。
4. 若一个文件同时包含通用框架与专有实现，应拆分文件：契约和框架留在蓝区，最小实现留在黄区。

保密边界不得以牺牲类型安全、测试能力或模块隔离为代价。应通过接口和装配解耦，而不是复制代码、使用全局变量或绕过现有注册机制。

## 4. 开发与验证要求

- 每个新需求都必须先创建独立 Git worktree，再创建或修改任何项目文件。该要求同样适用于应用代码、测试、配置、OpenSpec 提案／设计／规格／任务以及需求梳理记录；不得在主工作区先写文档、后补建 worktree。
- 只有用户明确要求修改仓库级 AGENTS.md 时，才允许在当前主工作区直接修改该文件；此例外不适用于任何其他需求产物。
- 保持改动聚焦，避免顺手修改无关特性。
- 保持现有公开 API、用户数据格式和持久化路径兼容；需要破坏性变更时必须先说明迁移方案。
- 所有 IPC 输入都要校验，channel 必须使用所属特性的命名空间。
- 日志不得包含凭据、配置原文、用户隐私或黄区实现细节。
- 新增架构能力需要相应的边界测试；修改业务功能需要相应的行为或回归测试。
- 不得提交与当前任务无关的本地文件、密钥、日志、构建产物或用户草稿。

## 5. 验证与推送

所有新需求（包括仅创建 OpenSpec 文档或需求梳理记录的需求）都必须在独立 Git worktree 中完成；主工作区仅用于人工查看、集成或用户明确授权的仓库级 AGENTS.md 修改。实现完成后，根据改动风险选择最小且适用的检查，不得默认执行全部验证。UI 或 Electron 改动本身不要求独立自动化验证任务、视觉验收或进程冒烟；仅在用户明确要求，或具体风险与验收标准确有需要时执行。

### Electron 图像验收方法

需要对 Electron 页面进行图像验收时，必须连接真实 Electron renderer，不得把普通浏览器打开的 renderer URL 当作等价验收环境。普通浏览器没有 RestX preload 与 IPC，可能出现 `window.restx` 不存在；macOS 的全屏 `screencapture` 又只覆盖当前 Space，即使 Accessibility 能找到其他 Space 中的 RestX 窗口，也可能只截到桌面壁纸。这两类结果都不构成有效图像证据。

推荐使用 electron-vite 内置的远程调试端口和 Chrome DevTools Protocol（CDP）：

1. 在当前需求 worktree 中，用未占用的独立端口启动真实应用：

   ```bash
   env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy \
     /absolute/path/to/node_modules/.bin/electron-vite dev --remoteDebuggingPort 9333
   ```

2. 等待终端出现 `DevTools listening on ws://127.0.0.1:9333/...`，再读取 `http://127.0.0.1:9333/json/list`，选择 `type === "page"` 且标题为 `RestX` 的 target，并连接其 `webSocketDebuggerUrl`。端口被占用时更换端口，不得复用用户正在调试的实例。
3. 通过 CDP 的 `Runtime.evaluate` 设置 `location.hash` 进入目标路由，并等待页面标题、加载态和关键控件达到预期。可使用同一接口触发只读或可逆的页面交互；不得在视觉验收中点击会写入真实用户数据的最终保存、删除或发送操作。需要验证写操作时必须使用隔离的测试数据目录。
4. 使用 `Page.captureScreenshot` 捕获真实 renderer。关键状态不在首屏时，先让目标元素执行 `scrollIntoView()` 再截图；响应式页面至少覆盖默认窗口首屏与关键交互状态。截图写入 `/private/tmp` 或其他临时目录，不得加入仓库。
5. 使用图像查看工具实际检查截图，不能只依赖 DOM 断言。验收至少关注：页面是否真实加载、文字是否截断或错位、背景与项目主题是否一致、连线和箭头是否可见、节点密度是否产生抵触感、滚动与响应式布局是否隐藏关键操作。DOM／CDP 数据可作为辅助证据，但不能替代图像检查。
6. 验收结束后关闭 CDP 连接并停止临时开发进程。记录目标路由、窗口尺寸、操作状态、截图路径、发现的问题及最终 `PASS`／`FAIL`。只有能看到 RestX 应用内容并经实际图像检查的截图才可标记为图像验收完成。

### 隔离与集成

- worktree 以需求为生命周期单位：同一条尚未完成的需求（含其 OpenSpec 文档、需求梳理记录、实现、修复和验证）复用同一个 worktree；任何新需求必须创建新的 worktree。
- worktree 只用于隔离开发和验证，不得在其中创建需求提交或推送远端。需求完成后，将变更整合到本地 `main`；只有本地 `main` 可以创建最终提交、同步远端并推送。
- 将 worktree 变更整合到 `main` 前，必须先确认该 worktree 没有无关文件或未完成需求；`main` 负责完成必要的冲突处理、检查和提交。
- 需求合入主干后，应删除对应的 worktree 和已合并本地分支；仅保留主工作区、进行中的需求 worktree，以及用户明确暂停待续的需求 worktree。放弃的需求经确认后也应清理。
- 新建 worktree 本身不复制 Git 对象历史；主要空间开销来自该 worktree 中的 `node_modules`、构建输出和安装包。因此只在验证要求需要时安装依赖或执行构建／打包，避免在每个 worktree 生成无须验收的 `dist` 产物。

### 推送条件

适用的检查通过后才允许推送。若用户表示要进行人工验收，还必须等待用户明确确认；若用户已明确授权“验证通过后直接推送”，则不重复询问。

## 6. 核心判断标准

一次改动只有同时满足以下条件，才符合 RestX 的开发宗旨：

- 新功能可以独立新增、禁用或删除；
- 不要求无关特性跟随修改；
- 公共层没有吸收特性专用逻辑；
- 蓝区只依赖公开契约，不泄露黄区实现；
- 安全边界、测试和现有用户行为仍然成立。
